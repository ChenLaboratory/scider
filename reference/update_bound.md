# Update the x,y limits of a plot

Update the x,y limits of a plot

## Usage

``` r
update_bound(p, x = NULL, y = NULL)
```

## Arguments

- p:

  A ggplot() object

- x, y:

  Vectors of new x and y limits.

## Value

a ggplot object
